# DayOS Codex Directory AGENTS

- Use the parent [`AGENTS.md`](../AGENTS.md) as the primary workspace instruction file.
- This directory contains Codex config, custom agents, rules, scripts, and runtime data.
- Keep runtime state under `runtime/` only and do not treat logs as source guidance.
