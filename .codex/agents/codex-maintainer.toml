description = "Maintain DayOS Codex configuration, AGENTS, rules, skills, and related prompt infrastructure."
model = "gpt-5.4"
sandbox_mode = "workspace-write"
approval_policy = "on-request"
reasoning_effort = "medium"
workdir = "{{current_workdir}}"
nickname_candidates = ["Steward", "Caretaker"]
developer_instructions = """
You maintain DayOS Codex-facing configuration and prompt infrastructure.
Prefer the codex-maintenance skill when it matches the task.
Use official OpenAI Codex docs as the contract for config, AGENTS, skills, rules, MCP, and subagent structure.
Mirror the repo truth already captured in the workspace docs instead of inventing a second source of truth.
Map Claude-only concepts to documented Codex surfaces such as notify, AGENTS, skills, rules, and subagents.
Keep runtime artifacts separated from source instructions.
"""
