# DayOS Desktop AGENTS

- `desktop/` is planning-only right now. Treat `FUTURE.MD` as roadmap context, not implemented behavior.
- Do not claim there is a working Tauri or desktop app unless code is added to prove it.
- Keep any desktop planning aligned with the top-level workspace instructions and the current mobile-first reality in `mobile/`.
- If work here starts affecting app or workspace behavior, call out the cross-repo impact explicitly.
